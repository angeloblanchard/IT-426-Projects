package shapes;

import javafx.scene.paint.Color;

public class Kirby extends Shape
{
    public Kirby(double x, double y, double thickness, Color color, boolean fill)
    {
        super(x, y, thickness, color, fill);
    }
}
